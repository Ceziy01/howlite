JWT_KEY = "<KEY>"
JWT_ALG = "<ALGORITHM>"
ACCESS_TOKEN_EXPIRES_MINUTES = <TIME_TO_EXPIRE>

POSTGRES_DB=<DB_NAME>
POSTGRES_USER=<DB_USER>
POSTGRES_PASSWORD=<DB_USER_PASSWORD>
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:${POSTGRES_PORT}/${POSTGRES_DB}